package org.firstinspires.ftc.teamcode.othersCode;

/**
 * Created by kskrueger on 12/7/17.
 */

public class JackTurn {
/*    double FL_speed = 0;
    double FR_speed = 0;
    double RL_speed = 0;
    double RR_speed = 0;
    double startCount = robot.rightFrontWheel.getCurrentPosition();

    target %= 360;

        while (startCount - encoderCounts>robot.rightFrontWheel.getCurrentPosition() && opModeIsActive())
    {
        angles = imu.getAngularOrientation(AxesReference.INTRINSIC, AxesOrder.ZYX, AngleUnit.DEGREES);

        double currentHeading = angles.firstAngle %= 360;  //Current direction
        double angleError = (target - currentHeading);
        angleError -= (360*Math.floor(0.5+((angleError)/360.0)));

        FL_speed = power - (error) / 25;  //Calculate speed for each side
        FR_speed = power - (error) / 25;
        RL_speed = power + (error) / 25;  //Calculate speed for each side
        RR_speed = power + (error) / 25;

        FL_speed = Range.clip(FL_speed, -1, 1);
        FR_speed = Range.clip(FR_speed, -1, 1);
        RL_speed = Range.clip(RL_speed, -1, 1);
        RR_speed = Range.clip(RR_speed, -1, 1);

        robot.leftFrontWheel.setPower(-FL_speed);
        robot.rightFrontWheel.setPower(FR_speed);
        robot.leftBackWheel.setPower(RL_speed);
        robot.rightBackWheel.setPower(-RR_speed);
    }

    left(0);
    right(0);*/
}
